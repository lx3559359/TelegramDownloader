# v0.9.0 文件完整性与精准修复验证

## 验收范围

- v0.8.0 数据库幂等迁移与项目内数据保留。
- 全新、续传和既有文件的 SHA-256 基线。
- 缺失、大小异常、同尺寸篡改、读盘失败、取消和路径越界。
- 所选媒体冲突安全留档与精准重新下载。
- 1180×720 图形界面、控制器编排、关闭收敛与隐私日志。
- 便携包、安装包、签名双源在线更新与直启数据保留。

## 自动化与真实化验证

最终代码候选提交 `cd9a8cc8effe0536f643d389eed8034f8f00bb32` 在干净工作树连续执行三轮 `scripts/test.ps1`：

- 第 1 轮：506 passed，pytest 13.40 秒，脚本总耗时 14.45 秒，Ruff 通过。
- 第 2 轮：506 passed，pytest 13.22 秒，脚本总耗时 14.24 秒，Ruff 通过。
- 第 3 轮：506 passed，pytest 13.42 秒，脚本总耗时 14.50 秒，Ruff 通过。

发布前结构化自审覆盖 `cc2111c..cd9a8cc` 的完整差异。当前环境没有可用于子任务的审查代理，因此按 `requesting-code-review` 清单执行自审；发现并修复两个 Important 并发问题：活动整任务结束后精准修复可能不继续执行，以及修复下载期间取消不能暂停调度器。两项均先由失败测试复现，修复后相关 82 项测试通过；未发现未解决的 Critical 或 Important 问题，评估为可进入构建。

隔离真实数据 QA 使用 v0.8.0 恢复包中的任务数据库和两个真实下载文件，但不复制 `secrets.dat`、`settings.json` 或目录数据库。测试在当前项目 `.build-temp` 副本中完成：

- 2 个旧版完成媒体均成功建立 SHA-256 基线。
- 对较小文件翻转一个字节且保持大小不变，检测为 1 个 `hash_mismatch`。
- 仅该媒体进入修复，受控网关调用 1 次，损坏内容保留为 `.corrupt`，重下文件恢复原 SHA-256。
- 未选中的另 1 个媒体数据库记录与文件 SHA-256 完全不变。
- 恢复包原目录全部 6 个文件的前后 SHA-256 一致。

2026-08-16 卷状态复核：F 盘 `Healthy / OK`，用于开发、测试与发布；D 盘仍为 `Warning / Full Repair Needed`，本版不在 D 盘执行写入、构建或发布。

## 构建与发布证据

功能分支构建于提交 `68b6232bf5cdcc7ee33898335eca24efc157ef01` 完成：

- `scripts/build.ps1` 输出 `PACKAGED_SMOKE_OK`，便携 ZIP 为 65,660,893 字节，SHA-256 为 `5c116259e58524b731e83aff052137db13be027a0b2b632b4f04d6375563d7ef`。
- `scripts/build-installer.ps1 -SkipAppBuild` 输出 `INSTALLER_SMOKE_OK`，安装包为 47,868,507 字节，SHA-256 为 `4cd374dcc4f211782e9b4093025e096b8273504e1df1ae3773c46a9364a25667`。
- 便携 ZIP 共 192 个条目；针对 `data/`、`downloads/`、数据库、会话、日志、`.corrupt*`、凭据、发布密钥的扫描命中为 0。
- 构建完成后再次执行完整门禁：506 项测试通过，Ruff 通过。

正式产物 URL、字节数、SHA-256、标签提交、签名清单验证、GitHub / 魔搭指针与直启数据保留结果将在双源发布完成后记录；发布标签保持指向发布代码提交，发布后证据仅追加到源码分支。
