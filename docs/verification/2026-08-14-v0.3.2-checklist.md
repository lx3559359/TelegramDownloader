# v0.3.2 本地候选版验证记录

日期：2026-08-14
分支：`codex/fix-content-reconnect`
状态：本地候选版；未发布到 GitHub、魔塔或在线更新清单

## 自动化验证

- `powershell -ExecutionPolicy Bypass -File scripts/test.ps1`
  - 结果：`291 passed in 46.44s`
  - Ruff：`All checks passed!`
- `powershell -ExecutionPolicy Bypass -File scripts/build-installer.ps1`
  - 结果：退出码 `0`，耗时约 `230.9s`
  - 流水线重新执行完整测试、PyInstaller 便携版构建、自检、Inno Setup 安装包构建、静默安装/升级/卸载烟雾测试。
  - 安装器明确拒绝 C 盘目标；项目盘测试安装成功。
  - 测试升级保留数据库、缩略图和哨兵文件；普通卸载移除受管程序文件但保留用户数据。
- 打包目录敏感文件检查：`0` 个 `tasks.sqlite3`、`catalog.sqlite3`、`secrets.dat` 或 `app.log` 被混入发布包。
- 隔离 GUI 数据副本数据库检查：`tasks.sqlite3=ok`、`catalog.sqlite3=ok`。

## Windows 图形界面验证

验证使用工作树 `.build-temp` 下的隔离运行副本；正式 v0.3.1 进程保持运行，正式应用目录未被替换或写入。

- 最终 EXE 可直接启动，界面显示 `v0.3.2 · stable`，既有已完成任务仍显示为 `2 / 2`、`27.8 MB`。
- 账号内容页可自动恢复 Telegram 连接，并在约 6 秒内完成 4 个群组/频道同步，不再停留在“正在同步”。
- 手动“刷新”可完成，旧群组列表不会在刷新期间被清空。
- 选择群组后恢复该群最近一次关键词和筛选条件；搜索记录页同时显示旧的“不完整”记录和本次“已完成”记录。
- 搜索框可正常聚焦和输入；提交普通关键词后完成远端搜索，并保存一条结果数为 0 的已完成记录。
- 在账号内容搜索框输入 `https://t.me/Zhangzhoulao66/56156?single` 后，程序切换到任务中心并规范化为 `https://t.me/Zhangzhoulao66/56156`。
- 指定链接成功扫描到 2 项媒体、已知大小 27.8 MB，并显示加入队列确认框；验证时选择 `No`，没有重复创建或下载任务。
- 登录页回填已保存的 API ID；API Hash 以密码圆点遮罩显示；代理类型和字段按保存配置恢复。二维码请求发生网络失败后，页面返回凭据页且字段未被清空。
- 设置页可打开，显示“配置会保存在应用目录”，在线更新开关和代理配置入口存在；未保存任何测试改动。
- 最终构建后的 EXE 再次完成启动、登录回填和设置入口检查。最后一项代码变更仅收紧畸形 URL 解析；最终完整测试与安装包流水线均在该变更之后重新通过。
- 隔离副本测试结束后，正式 v0.3.1 再次手动刷新群组并显示“同步完成”，4 个群组/频道仍在，确认正式 Telegram 会话未受并行测试影响。

## 项目内路径验证

- 构建、工具缓存、临时用户配置、安装烟雾测试、GUI 测试副本均位于 `D:\Codex Project\Telegram下载器\.worktrees\content-reconnect` 下。
- 安装器烟雾测试中的 `TEMP`、`TMP`、`APPDATA`、`LOCALAPPDATA`、安装目录和自检写入路径全部重定向到工作树内。
- 发布包不包含用户数据库、凭据、日志或下载文件；首次运行时在可执行文件所在应用目录创建 `data`、`downloads`、日志、缓存和更新目录。

## 候选产物

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `dist/TelegramDownloader-0.3.2-win-x64-portable.zip` | 65,536,689 | `9A0C5ECCFB6ACA0EFF313C68DB0C2AA97DD1B0FF60CEA1F7F918FF1CCAB1E59F` |
| `dist/release/TelegramDownloader-0.3.2-win-x64-setup.exe` | 47,740,628 | `2FA03685428B3C457ABF6FC5EAABE97E27DD235946623DF1B03834A270661A96` |

## 发布边界

- 未合并到 `main`。
- 未推送 GitHub 或魔塔。
- 未创建远程 tag、GitHub Release 或魔塔发布。
- 未提升签名在线更新清单；现有用户不会自动收到此候选版。
