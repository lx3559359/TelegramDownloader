# TelegramDownloader v0.4.2 真实化全链路验证记录

日期：2026-08-15

分支：`codex/real-qa-v042`

代码基线：`08c6069`（验证文档提交另计）

状态：验证完成，已本地快进合并至 `main`；未推送、未对外发布、未更新线上清单

## 本轮修复与优化

- 下载调度器关闭宽限期由固定 5 秒改为可配置值，生产默认 30 秒；活动下载先接收暂停信号并等待 Telegram 连接正常归还，超时后才强制取消。
- 搜索结果底部明确显示已入队及不可用数量；没有可加入项目时禁用“全选”和“反选”，避免点击后无反馈。
- 任务按钮按选中任务状态启用，仅展示当前真正可执行的暂停、继续、重试和打开操作。
- 在线更新先验证双源签名并完成版本协调，再执行新版本安装策略；签名有效但低于当前版本的稳定版现在返回“无更新”，不再误报源无效。
- 保留已实现的会话复用、群组刷新反馈、搜索进度、并发缩略图、大图预览、双击预览、选择性下载、断点续传、单实例和项目内数据隔离。

## 真实账号与图形交互验证

- 使用项目运行目录内已有加密凭据和会话重复启动，均直接恢复登录，没有再次显示二维码。
- 真实同步及手动刷新完成，共得到 4 个可浏览会话；刷新阶段、已发现数量和完成状态均可见。
- 通过真实 Telegram 消息链接完成扫描，任务成功进入本地任务库；取消扫描不会误删已有任务。
- 在真实会话中按日期、媒体类型和关键词搜索得到 15 项结果，搜索进度可见，双击缩略图成功打开应用内大图预览。
- 15 项结果均已在队列时，界面显示“已选 0 项 · 15 项已入队”，且“全选”“反选”保持禁用。
- 对真实媒体任务执行继续后，已下载字节数增加 524,288；随后暂停成功。
- 用独立进程模拟下载中崩溃并以退出码 23 终止，重启后任务由下载中恢复为排队并自动续传，已有字节未丢失。
- 恢复后立即暂停并退出，调度器在 15.56 秒内完成收尾；15 个媒体项最终全部暂停，未残留异步任务，Telegram 网关已断开，也未产生连接协程警告。
- 最终打包 EXE 直接启动成功，窗口标题为“Telegram 下载器”，正常关闭后退出码为 0，未新增错误日志。

## 自动化与构建验证

- 完整测试：`350 passed in 8.71s`。
- 静态检查：`ruff check src tests scripts` 返回 `All checks passed!`。
- 便携版构建：退出码 0，构建过程重新执行完整测试和 Ruff，`PACKAGED_SMOKE_OK`。
- 安装包构建：退出码 0，重新执行完整测试和 Ruff，`PACKAGED_SMOKE_OK`、`INSTALLER_SMOKE_OK`。
- 安装器静默安装、覆盖升级、卸载和数据保留检查通过；安装目标及测试临时目录均位于项目工作树。
- 构建前后即时比对项目运行数据：26 个文件、10,181,191 字节、聚合 SHA-256 `58554E49E06225AFCDFB78AED55E70667F528A51C29EABA952F768FCE5FD9E89`，完全一致。
- 便携 ZIP 共 192 个条目，不包含 `data/` 或 `downloads/`，不会把本地账号会话和下载内容打入发布包。

## 在线更新验证

- GitHub 与魔塔双源均成功读取并验证已签名的 `0.4.0` 稳定版清单。
- 本地版本为 `0.4.2` 时，结果为 `blocked=false`、`offered_version=null`，两个更新源均保持可用。
- 自动化回归同时覆盖“较旧签名版本不阻断”和“较新版本要求更高更新器时仍阻断”两条策略路径。

## 项目内路径与隐私验证

- 运行根目录：`D:\Codex Project\Telegram下载器\.worktrees\content-ux\dist\TelegramDownloader`。
- 设置、DPAPI 加密凭据、会话、任务数据库、内容目录库、日志、缩略图、临时文件、下载目录以及更新 staging/backup/helper/journal 全部位于运行根目录内。
- 构建和安装测试的缓存、临时目录、应用数据目录及安装目录均重定向到 D 盘项目工作树；应用业务数据不写入 C 盘用户目录。
- 验证记录不包含 API 凭据、手机号、会话内容、群名、搜索关键词或消息正文。

## 最终候选产物

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `dist/TelegramDownloader/TelegramDownloader.exe` | 5,708,505 | `1B59313CA9C2AA3F3CFF54023475F044533331ABF740EB505398193739D3D32A` |
| `dist/TelegramDownloader-0.4.2-win-x64-portable.zip` | 65,561,493 | `88ECD3B624997233818287B7DFE4BB45B27FE2AE62C6F5409C73C3831A29E61E` |
| `dist/release/TelegramDownloader-0.4.2-win-x64-setup.exe` | 47,766,142 | `BD51091F8D527B9C88A00C7057147FC4E5826ABCA93EF5CD69BC46BB5F5EA282` |

## 发布边界

- 本轮只进行本地快进合并。
- 不推送 GitHub 或魔塔，不创建 tag 或 Release。
- 不提升签名在线更新清单，现有用户不会自动收到此候选版。
