# TelegramDownloader v0.12.1 搜索限流恢复验收记录

## 发布范围

- 基线：已公开的 v0.12.0 全账号搜索版本。
- 修复：短期限流倒计时与同游标重试、等待期间取消、长等待可恢复、未完成搜索继续执行、脱敏诊断日志。
- 版本：0.12.1；数据库 schema 保持 4，在线更新协议不变。

## 合并验证

本次实现先在 v0.11.2 后续基线上完成，再合并到已经发布 v0.12.0 的最新 `main`。冲突解决保留了 v0.12.0 的全账号复合游标、稀疏分页、真实来源和跨会话相册隔离，并将限流恢复放在每次远端分页请求外层。

| 检查 | 结果 |
| --- | --- |
| 搜索服务、控制器、账号内容 UI 与全账号端到端专项 | `134 passed in 4.25s` |
| 合并后完整 pytest 套件 | `698 passed in 29.88s` |
| Ruff | `All checks passed!` |
| 冲突标记与差异格式检查 | 通过 |

自动化覆盖 500 条结果跨 5 个分页、两个间歇短限流、同游标重试、长等待保留已有结果、第三次短限流停止等待、倒计时取消、控制器错误展示和“继续搜索”界面状态。

## 正式发布

正式发布提交为 `6875fb57df98d2e7c21b9bee2be6afbdaf50f5a2`。GitHub 与魔搭的 `v0.12.1` annotated tag 剥离后均指向该提交；发布时 GitHub `main` 与魔搭 `source` 也均指向该提交。

`scripts/release/release.ps1 -Version 0.12.1` 以退出码 0 返回 `RELEASE_PUBLISHED v0.12.1`。发布事务内第一轮完整套件为 `698 passed in 28.19s`，构建门禁再次得到 `698 passed in 27.91s`，两轮 Ruff 均为 `All checks passed!`。冻结程序构建返回 `PACKAGED_SMOKE_OK`，安装器构建、非系统盘安装、升级、卸载数据保留和冻结自检返回 `INSTALLER_SMOKE_OK`。

| 产物 | 字节 | SHA-256 |
| --- | ---: | --- |
| `TelegramDownloader-0.12.1-source.zip` | 803,150 | `2efae5d18c5c59e12d07c4f1ebab6ca4e0ded527ac25b4d524fa13446af11ac3` |
| `TelegramDownloader-0.12.1-win-x64-portable.zip` | 65,740,117 | `b2c8611b032a2d112888b128e44d39fbdefa96ced7764969cafe46b2b5469e9a` |
| `TelegramDownloader-0.12.1-win-x64-setup.exe` | 47,947,481 | `eba9c9047bf32b0e45a1631e729b91077cb7010938feb5ba28842becb885eafb` |
| `update-manifest.json` | 4,052 | `0dc5e9d8d78e324b2bd8befb5720aac4f54c8558cbd833489537622c553b5d20` |
| `update-manifest.sig` | 89 | `9e40adeac9fcb40a729ecadfb3597d159dd31a02e5afcaae81652edda9a977f8` |

独立发布后复核结果：

- GitHub Release 已公开，`isDraft=false`、`isPrerelease=false`，`releases/latest` 返回 `v0.12.1`，7 个资产名称、大小和 GitHub 记录的 SHA-256 与本地一致。
- GitHub `main`、魔搭 `source` 以及两端 `v0.12.1` 标签剥离目标均确认发布提交不变。
- 从 GitHub 和魔搭重新下载的正式资产与本地候选逐字节一致；两端 `latest.json` 都是 58 字节，SHA-256 均为 `c1c750bdbb92a1b0ec61e5097092d575499b7b2922cfe7c6e18a80b8e8034e67`。
- 使用内置可信公钥重新验证 Ed25519 清单签名成功，版本为 0.12.1，密钥标识为 `release-2026-01`；运行包和安装包再次按清单验证大小与 SHA-256。
- 冻结程序重新执行 `--self-test`：`ok=true`、版本 0.12.1、catalog schema 4，所有写入路径仍位于冻结程序根目录。
- 以 0.12.0 为当前版本执行真实 `UpdateCoordinator` 双源检查：GitHub 与魔搭均为 `valid`，发现版本 0.12.1，`blocked=false`。

- GitHub：https://github.com/lx3559359/TelegramDownloader/releases/tag/v0.12.1
- 魔搭：https://modelscope.cn/models/lx3559359/TelegramDownloader
