# TelegramDownloader v0.12.1 搜索限流恢复验收记录

## 发布范围

- 基线：已公开的 v0.12.0 全账号搜索版本。
- 修复：短期限流倒计时与同游标重试、等待期间取消、长等待可恢复、未完成搜索继续执行、脱敏诊断日志。
- 版本：0.12.1；数据库 schema 保持 4，在线更新协议不变。

## 合并验证

本次实现先在 v0.11.2 后续基线上完成，再合并到已经发布 v0.12.0 的最新 `main`。冲突解决保留了 v0.12.0 的全账号复合游标、稀疏分页、真实来源和跨会话相册隔离，并将限流恢复放在每次远端分页请求外层。

| 检查 | 结果 |
| --- | --- |
| 搜索服务、控制器、账号内容 UI 与全账号端到端专项 | `134 passed in 4.25s` |
| 合并后完整 pytest 套件 | `698 passed in 29.88s` |
| Ruff | `All checks passed!` |
| 冲突标记与差异格式检查 | 通过 |

自动化覆盖 500 条结果跨 5 个分页、两个间歇短限流、同游标重试、长等待保留已有结果、第三次短限流停止等待、倒计时取消、控制器错误展示和“继续搜索”界面状态。

## 正式发布

正式构建、安装器冒烟、签名资产哈希、双源发布与远端更新发现结果将在发布事务成功后记录；发布前不预填公网结果。
