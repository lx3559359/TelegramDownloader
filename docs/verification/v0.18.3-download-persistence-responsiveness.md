# TG 快取 v0.18.3 下载持久化响应验证

验证日期：2026-08-24

验证分支：`codex/download-persistence-responsiveness-impl`

当前已验证实现提交：`b887fc8`

## 当前结论

- 下载进度、终态和调度状态已迁移到共享后台持久化协调器。
- 下载相关联合回归为 273 项通过，用时 22.85 秒。
- 修复一处旧测试的空让步等待后，完整自动化为 1,171 项通过，用时 125.44 秒；Ruff 全部通过。
- 七次离线合成基准全部通过；人工仓库调用每次延迟 50 ms 时，事件循环最大间隔为 5.00–5.48 ms，媒体持久化写入每次均为 3，最终字节均为 20，终态均已耐久提交。
- Qt/qasync 合成响应测试通过；使用临时应用目录和 QTimer，未创建 Telegram 网关或连接网络。

## 已执行验证

### 聚焦与联合回归

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_repository.py tests/test_download_persistence.py tests/test_downloader.py tests/test_scheduler.py tests/test_download_queue_e2e.py tests/test_download_queue_stress.py tests/test_controller.py tests/test_app.py tests/test_file_integrity_e2e.py -q
```

- 结果：273 项通过，用时 22.85 秒。

### 完整自动化

```powershell
.\scripts\test.ps1
```

- 首次结果：1,169 项通过、2 项失败；失败均为 `tests/test_background_runtime_e2e.py` 使用固定 `100 × sleep(0)`，无法等待新增的后台数据库调度键读取。
- 对测试助手改用 1 秒墙钟条件等待后，该文件连续三轮均为 3 项通过。
- 重跑结果：1,171 项通过，用时 125.44 秒；Ruff 为 `All checks passed!`。

### 七次合成基准

```powershell
1..7 | ForEach-Object { .\.venv\Scripts\python.exe scripts\benchmark_download_persistence.py }
```

| 轮次 | 最大事件循环间隔 | 媒体写入 | 最终字节 | 终态耐久 |
| ---: | ---: | ---: | ---: | :---: |
| 1 | 5.39 ms | 3 | 20 | true |
| 2 | 5.01 ms | 3 | 20 | true |
| 3 | 5.00 ms | 3 | 20 | true |
| 4 | 5.48 ms | 3 | 20 | true |
| 5 | 5.35 ms | 3 | 20 | true |
| 6 | 5.42 ms | 3 | 20 | true |
| 7 | 5.01 ms | 3 | 20 | true |

七轮均输出 `DOWNLOAD_PERSISTENCE_BENCHMARK_OK`。基准只使用 `TemporaryDirectory`、固定 20 个合成字节、`SyntheticGateway` 和 `SyntheticRepository`。

### GUI 合成响应

```powershell
$env:QT_QPA_PLATFORM='offscreen'
.\.venv\Scripts\python.exe -m pytest tests/ui/test_download_persistence_responsiveness.py -q
```

- 结果：1 项通过。
- 50 ms 批量写与受控终态提交期间，5 ms 精确定时器持续触发；终态 future 在仓库释放前保持未完成。

## 尚未执行

- 三轮最终自检：未执行。
- 便携包构建与 `PACKAGED_SMOKE_OK`：未执行。
- 安装器构建与 `INSTALLER_SMOKE_OK`：未执行。
- 包隐私检查：未执行。
- 便携自检：未执行。
- D 盘安装、原位升级、普通卸载数据保留：未执行。
- 最终便携包和安装器大小、SHA-256：未生成。
- 远端标签、上传与在线更新指针：未执行，也未获本轮授权。

## 隐私边界

- 自动化和基准仅使用临时目录、固定合成媒体和本地假仓库。
- 验证记录只包含测试数量、耗时、固定字节数、提交标识和后续制品校验值，不记录账号、会话、消息、搜索词、文件名、用户下载路径或凭据。
- 当前工作仅形成本地 v0.18.3 候选，不推送、不打标签、不修改 GitHub 或魔搭在线状态。
