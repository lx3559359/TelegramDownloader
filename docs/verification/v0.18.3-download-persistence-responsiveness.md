# TG 快取 v0.18.3 下载持久化响应验证

验证日期：2026-08-24

验证分支：`codex/download-persistence-responsiveness-impl`

当前已验证实现提交：`7c212c0`

## 当前结论

- 下载进度、终态和调度状态已迁移到一个共享后台持久化协调器；普通进度合并写入，暂停、失败、完成等边界等待耐久提交。
- 三轮最终自检已覆盖聚焦回归、完整自动化、七次合成基准、便携构建与自检、隐私检查、安装/原位升级/卸载保留、Qt 响应测试及人工 GUI 冒烟。
- 最终提交前再次执行完整自动化：1,171 项通过，用时 107.98 秒；Ruff 无错误。
- 所有验证均使用合成数据、临时目录或工作树内隔离目录，不读取真实 Telegram 账号、会话、消息或下载数据。
- 当前只形成本地 v0.18.3 候选；未合并主分支、未打标签、未推送、未发布在线更新。

## 第 1 轮：代码、回归与响应基准

### 聚焦回归

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_download_persistence.py tests/test_downloader.py tests/test_scheduler.py tests/test_download_queue_e2e.py -q
```

- 结果：74 项通过，用时 3.04 秒。

### 完整自动化

```powershell
.\scripts\test.ps1
```

- 结果：1,171 项通过，用时 53.22 秒；Ruff 输出 `All checks passed!`。

### 七次合成基准

```powershell
1..7 | ForEach-Object { .\.venv\Scripts\python.exe scripts\benchmark_download_persistence.py }
```

| 轮次 | 最大事件循环间隔 | 媒体写入 | 最终字节 | 终态耐久 |
| ---: | ---: | ---: | ---: | :---: |
| 1 | 5.02 ms | 3 | 20 | true |
| 2 | 5.04 ms | 3 | 20 | true |
| 3 | 5.04 ms | 3 | 20 | true |
| 4 | 5.02 ms | 3 | 20 | true |
| 5 | 5.01 ms | 3 | 20 | true |
| 6 | 5.02 ms | 3 | 20 | true |
| 7 | 5.05 ms | 3 | 20 | true |

七轮均输出 `DOWNLOAD_PERSISTENCE_BENCHMARK_OK`。基准只使用 `TemporaryDirectory`、固定 20 个合成字节、`SyntheticGateway` 和 `SyntheticRepository`。

## 第 2 轮：便携构建、隐私与冻结程序自检

```powershell
.\scripts\build.ps1
.\scripts\smoke.ps1
```

- 首次构建在并行构建负载下暴露一个旧控制器测试的 20 ms 硬编码定时假设：1,170 项通过、1 项失败。测试改为由后台条件确定性释放后连续执行 10 次均通过，修复提交为 `7c212c0`。
- 重跑构建：内部完整自动化 1,171 项通过，用时 67.62 秒；Ruff 通过；PyInstaller 构建成功；构建内与显式烟雾测试均输出 `PACKAGED_SMOKE_OK`。
- 便携 ZIP 共 195 个条目；`data/`、`downloads/`、数据库、凭据、日志、`.part` 和 `.corrupt*` 等禁止内容为 0。
- 从工作树内独立 D 盘目录解压后执行冻结程序 `--self-test`：退出码 0、版本 0.18.3、`ok=true`、全部组件为 true；14 个可写路径全部位于隔离根目录内。

制品：

| 制品 | 字节 | SHA-256 |
| --- | ---: | --- |
| `dist/TelegramDownloader-0.18.3-win-x64-portable.zip` | 66,019,374 | `EC09AF68663DFEA2C97AC568914F602B216439482CDE3FE7270FBBC6879D6A9A` |
| `dist/TelegramDownloader/TelegramDownloader.exe` | 6,119,149 | `8C53CBB5C8900C11BBD90B4D7262EFB0AF0E02D4546E864A32D255B60869D715` |

## 第 3 轮：安装生命周期与人工 GUI

```powershell
.\scripts\build-installer.ps1 -SkipAppBuild
$env:QT_QPA_PLATFORM='offscreen'
.\.venv\Scripts\python.exe -m pytest tests/ui/test_download_persistence_responsiveness.py -q
```

- Inno Setup 编译成功，安装器脚本输出 `INSTALLER_SMOKE_OK`。
- 安装生命周期验证确认：拒绝 C 盘目标；允许工作树内 D 盘目标；安装后自检通过；同版本原位升级后合成目录、缩略图和哨兵文件哈希不变；普通卸载后这三类用户数据仍保留，受管理运行文件已移除。
- Qt/qasync 离屏响应测试 1 项通过，用时 2.63 秒；50 ms 批量写与受控终态提交期间，5 ms 定时器持续触发，终态 future 在仓库释放前保持未完成。
- 通过桌面自动化启动隔离安装实例，确认登录指导页和 `v0.18.3 · stable`；主窗口可移动；“维护中心”和“设置”可打开；关闭主窗口后进程继续在托盘运行；再次启动同一可执行文件时恢复原有单实例窗口。
- 当前桌面自动化无法直接定位 Windows 通知区域小图标，因此未人工点击托盘右键菜单；托盘菜单由现有 Qt 自动化覆盖，本轮人工覆盖的是关闭到托盘及单实例恢复。测试进程随后按已核验的绝对路径停止，隔离运行文件正常卸载，测试数据保留。

安装器：

| 制品 | 字节 | SHA-256 |
| --- | ---: | --- |
| `dist/release/TelegramDownloader-0.18.3-win-x64-setup.exe` | 48,234,838 | `C06F311105562EC7D8A3D946DF02E1C3285A0CC5CED8BDB4D24AD0AD79FD5785` |

## 最终提交前验证

以下命令在本文证据更新后执行并记录：

```powershell
git diff --check
.\scripts\test.ps1
git status --short
```

- 结果：`git diff --check` 无空白错误；完整自动化 1,171 项通过，用时 107.98 秒；Ruff 输出 `All checks passed!`；除本文证据更新外无未提交改动。

## 隐私与发布边界

- 自动化和基准仅使用临时目录、固定合成媒体和本地假仓库。
- 验证记录只包含测试数量、耗时、固定字节数、提交标识和制品校验值，不记录账号、会话、消息、搜索词、用户文件名、用户下载路径或凭据。
- 远端标签、上传与在线更新指针均未执行，也未获本轮授权。
