# v0.11.0 下载资源控制验证

## 验证范围

- 单活动任务、稳定 FIFO、持久优先级、等待暂停、恢复与安全关闭。
- 1–5 文件并发实时调整及全部媒体共享的总下载限速。
- 任务队列位置、调度摘要、设置即时生效和等待任务刷新抑制。
- v0.10.0 数据副本迁移、断点续传、SHA-256 完整性及非 C 盘路径边界。
- Windows 直启目录、便携 ZIP、安装包及 GitHub/魔搭双源签名更新。

## 发布前门禁

以下证据只在对应命令实际完成后填写，不预填测试数量、耗时、资产大小、哈希、发布地址或远端状态。

### 开发验证第 1 轮

- 基线功能提交：`9b461f2`；发布候选文档与版本改动尚未提交时执行。
- `scripts/test.ps1`：633 项测试通过，耗时 16.54 秒；Ruff 全项目检查通过，命令退出码为 0。
- 首次运行准确发现一处仍断言 v0.10.0 的窗口测试；更新发布契约后重跑全门禁通过。

### 结构化审查与第 2 轮

- 审查范围为 `bde8fe4..5240bd6`，覆盖调度活性、Future 收敛、取消、限流计量、SQLite 事务与迁移、旧设置位置参数、Qt 选择状态、刷新放大及路径边界。
- 发现并修复 1 组 Important 问题：已有带宽等待不会响应实时限速变更；暂停期间仍可能多写一个媒体块；取消的带宽预留会让恢复后的请求额外等待。每个边界均先增加失败回归，再由 `332b263` 修复；暂停/关闭现在约每 50 ms 检查一次，配置变更会唤醒等待者，取消会回收并重新计算共享预留。
- 未发现未解决的 Critical 或 Important 问题；SQLite 只发生预期加列，控制器等待任务不再各自刷新数据库，Qt 优先按钮只对单个未归档等待任务启用。
- 第 2 轮开始前工作树干净，固定提交为 `332b263a4531473a25061e77c1ded4c7d233a966`；`scripts/test.ps1` 为 638 项通过，耗时 21.05 秒，Ruff 通过，退出码 0。

### 压力、迁移与真人化交互

- 新增真实 SQLite 压力回归：50 个任务、500 个媒体项，包含 5 次活动任务重复提交、10 次连续优先级调整、等待/活动暂停、1→5→1→5 文件并发及 256 KiB/s→不限速→256 KiB/s→不限速切换；最大同时活动任务为 1，最大文件并发为 5，恢复后 50/500 全部完成且 ID 唯一。
- 从 F 盘 v0.10.0 直启目录只复制 `data` 与 `downloads` 到工作树 `.build-temp` 隔离根；迁移前为 10 个文件、1 个任务、2 个媒体项和 6 张内容库表。v0.11.0 自检后行数全部相同，除任务库预期加列及自检报告刷新外，既有文件哈希变化为 0；原 v0.10.0 目录变化为 0，13 个可写路径全部位于 F 盘隔离根。
- DPAPI 凭据在副本中可正常读取但未输出；真实 Telegram 只读连接确认 `connected=True`、`authorized=True`，无需重新扫码。
- 源码 GUI 成功显示 v0.11.0、调度摘要和“优先下载”。Windows 自动化首次激活被桌面会话拒绝，恢复后只读可访问性树检查通过；未执行不可靠的坐标点击，交互动作继续由 Qt 控件/信号测试覆盖。

### 构建与第 3 轮

- 工作树候选构建输出 `PACKAGED_SMOKE_OK` 与 `INSTALLER_SMOKE_OK`；构建内门禁为 639 项测试通过、Ruff 通过，耗时 23.60 秒。
- 候选便携 ZIP 共 192 个条目，用户 `data`/`downloads`、设置、凭据、会话、数据库、日志、诊断包、`.part`、`.corrupt*`、更新备份和发布私钥命中为 0。
- 候选冻结程序 `--self-test` 通过：版本 0.11.0，13 个可写路径全部位于 F 盘运行根，根外路径为 0。
- 候选便携包为 65,722,017 字节，SHA-256 `DEA53BC68AD75911D7AF15518D08BE88E33E087AA7A5071BE42C14FD3D07CA25`；候选安装包为 47,928,865 字节，SHA-256 `6681576F83E73FBE956C769DBB23B2940578456595D8E2477E6944763CDC4D55`。正式发布事务会重新构建，发布证据只记录届时重新测得的正式值。
- 功能分支快进合并到本地 `main` 提交 `10275956940f20f332478f80bed3cd66b56d38bd`；合并后 `scripts/test.ps1` 为 639 项通过，耗时 26.27 秒，Ruff 通过。
- 主分支重新构建输出 `PACKAGED_SMOKE_OK`，随后新建 `dist/release/v0.11.0-portable`；只继承 v0.10.0 的 `data` 与 `downloads`，共核对 199 个运行/数据文件。旧目录哈希变化为 0，迁移后仍为 1 个任务、2 个媒体项和 6 张内容库表，除预期任务库加列与自检报告外无既有文件变化。
- v0.11.0 直启程序冻结自检通过：版本 0.11.0、13 个可写路径全部位于 F 盘直启根；设置、DPAPI 凭据、两个数据库和下载文件共 6 个受保护文件在冻结自检前后哈希变化为 0。
- 直启数据副本执行真实只读 Telegram 会话复用检查，结果为连接成功且已授权，无需重新扫码；检查未输出账号、群组、链接或凭据。

## 发布证据

- 待记录：正式发布事务、标签提交、GitHub/魔搭分支与标签核对。
- 待记录：正式资产大小、SHA-256、Ed25519 验签、远端逐字节比对和在线更新发现。
