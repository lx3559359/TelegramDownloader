# v0.18.1 二维码立即过期修复验证

验证日期：2026-08-23  
实现提交：`2be6e02551df469687017a11d99e14bffaa36fb3`

## 修复范围

- Telegram 网关在收到二维码时计算相对于响应时刻的有效秒数。
- 控制器拒绝剩余时间不足 5 秒或非有限数值的二维码；最多安全重建一次后再展示。
- 登录窗口使用单调时钟倒计时，不再直接比较系统墙上时间。
- 界面到期、Telegram 超时和手动刷新共享 generation 与刷新锁；旧代次事件会被忽略。
- 二维码等待任务先于界面展示启动，并显式绑定创建它的登录网关。
- 候选账号二维码刷新失败不会替换或断开当前账号服务。

## 定向测试

最终定向认证集命令：

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/test_gateway.py tests/test_controller.py tests/test_app.py tests/test_account_access.py tests/ui/test_login_dialog.py -q --basetemp=.build-temp/pytest-task7-focused
```

结果：`228 passed in 11.54s`。覆盖相对有效期、短 TTL 拒绝、非有限 TTL、监听顺序、单调倒计时、单次到期信号、并发刷新去重、手机号、两步验证、候选账号隔离、应用信号接线和登录成功会话保存。

## 完整测试与 Ruff

静态检查命令：

```powershell
& '.\.venv\Scripts\python.exe' -m ruff check src tests scripts
```

结果：`All checks passed!`

三轮完整测试均由最终候选源码执行：

1. 独立完整测试：`1113 passed in 214.33s (0:03:34)`。
2. `scripts/build.ps1` 内置测试：`1113 passed in 67.64s (0:01:07)`，随后 Ruff 通过。
3. `scripts/build-installer.ps1` 内置测试：`1113 passed in 213.98s (0:03:33)`，随后 Ruff 通过。

三轮均无失败、错误或未处理后台任务报告。

## 真实 Telegram 生命周期验证

使用本机已有的 DPAPI 加密 API 凭据，在无保存会话、无账号身份读取的隔离探针中真实请求二维码并等待自然到期。探针只输出固定标记和数值：

```text
FIRST_TTL_SECONDS=29
FIRST_LABEL_POSITIVE=True
DISPLAY_COUNT=2
SECOND_TTL_SECONDS=29
REFRESH_COUNT=1
PARALLEL_WAIT_PEAK=1
QR_TOKEN_OUTPUT=False
LIVE_QR_RECOVERY_OK
```

结果说明：首次二维码不是立即过期；到期后只执行一次刷新并展示第二个有效二维码；任一时刻最多一个等待任务。

## 便携版与安装版冒烟测试

- `scripts/build.ps1`：输出 `PACKAGED_SMOKE_OK`。
- `scripts/build-installer.ps1`：再次输出 `PACKAGED_SMOKE_OK`，Inno Setup 7.0.2 编译成功，并输出 `INSTALLER_SMOKE_OK`。
- 便携 ZIP：`67,579,154` 字节；SHA-256 `a1c753a6c57cac9b746f26f88ba4aae34f3426ee2251a32916c0b905afec5539`。
- 安装程序：`48,202,741` 字节；SHA-256 `c5a5fcc80492943d3c4cd1046d727ff604eb676912717812726fad3e9dfb88a1`。
- 直启 EXE：`6,091,688` 字节；SHA-256 `6088e1141d6deb7b32462693f60dcc670134b1961eeb4fb3137bcfacd00effe1`。
- 便携 ZIP 共 `195` 个条目；`data`、`downloads`、设置、数据库、日志、`secrets.dat`、分片和损坏隔离文件的禁入命中数为 `0`。

## 隐私边界

验证记录、命令输出和提交文件未记录二维码 URL/令牌、API ID、API Hash、手机号、账号身份、确认码、两步验证密码、代理密码、Telegram 会话、群组名称、会话标题或消息内容。真实探针只读取本机已有的 DPAPI 加密凭据到进程内存，未复制到发布包或版本控制。

## 尚未执行的发布操作

本轮只生成本地 v0.18.1 候选产物；尚未合并到 `main`、推送远端、创建或移动 Git 标签、上传公开资产、签署在线更新清单，也未修改线上 stable 指针。只有用户后续明确授权后才执行发布操作。
