# TelegramDownloader v0.15.0 存储维护三轮验收记录

日期：2026-08-22

## 第一轮：规格、安全和定向回归

逐项对照存储维护设计，覆盖默认关闭与固定策略、状态 schema、更新事务保护、白名单扫描、执行前重验证、一次性确认、手动两次确认、活动互斥、自动调度、聚合通知、路径自检、应用关闭顺序和打包合同。

- 定向矩阵：`445 passed in 24.56s`。
- 新增冻结 CLI UTF-8 输出回归包含在 `tests/test_bootstrap.py` 中。
- `git diff --check`：通过。
- 测试使用项目内临时树与合成聚合数据，输出未包含真实 Telegram 账号、来源、任务或媒体路径。

## 第二轮：完整源码回归

| 检查 | 结果 |
| --- | --- |
| `scripts/test.ps1` 全量 pytest | `996 passed in 48.60s` |
| Ruff（`src`、`tests`） | `All checks passed!` |
| `python -m compileall -q src tests` | 通过 |
| `python -m pip check` | `No broken requirements found.` |

## 第三轮：冻结产物和安装器

- `scripts/build.ps1` 内再次执行全量回归：`996 passed in 52.02s`，Ruff 通过。
- 便携程序与更新健康检查：`PACKAGED_SMOKE_OK`。
- 安装、原位升级、在线更新运行时检查和普通卸载保留数据：`INSTALLER_SMOKE_OK`。
- 从最终便携 ZIP 解压到全新项目内临时目录后，直接通过管道解析 `TelegramDownloader.exe --self-test`：`ok=true`、版本 `0.15.0`、6 个运行组件可用、14 个公开受管路径均位于隔离运行根，`maintenance_state` 未逃逸。
- 最终 ZIP 逐条目检查 `data`、`downloads`、`storage-state.json`、`.part`、`.corrupt`、`app.log`、`tasks.sqlite3`、`catalog.sqlite3` 和 `secrets.dat`，受禁条目计数为 `0`。

第三轮首次执行发现冻结程序 stdout 在含中文路径时使用 CP936，PowerShell 7 按 UTF-8 解码后导致严格路径比较失败；磁盘自检报告与实际运行根正确。修复为自检 JSON 输出前显式配置 UTF-8，并新增失败回归测试；随后从第一轮开始完整重跑以上三轮，最终证据均来自修复后的候选源码和产物。

## 最终资产

| 资产 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `TelegramDownloader-0.15.0-win-x64-portable.zip` | 65,937,162 | `d827ca7e336f0c85c8562be5a44b45ce246afb62dd62dcbbda661dfd0d0e40cd` |
| `TelegramDownloader-0.15.0-win-x64-setup.exe` | 48,120,918 | `73c9e8cd34f10fd39db42114868769de39c19abdbdfd5f1bfe22892054c2c806` |

## 外部边界

自动化不会读取或输入真实 Telegram 凭据；真实账号权限、服务端内容与网络吞吐不属于本地自动化范围。不推送、不打标签、不发布，除非用户另行明确授权。
