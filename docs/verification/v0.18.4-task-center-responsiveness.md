# TG 快取 v0.18.4 任务中心响应验证

验证日期：2026-08-25

验证分支：`codex/task-center-large-data-responsiveness-impl`

性能实现提交：`9944cdd`

候选元数据提交：`2c8e78b`

## 当前结论

- 当前仅为本地候选；未合并主分支、未打标签、未推送、未发布在线更新。
- 三轮最终验收均已执行；源码门禁、便携包、冻结程序、安装器生命周期与 GUI 检查均通过。
- 第 1 轮重点回归 226 项通过，用时 23.43 秒；初始完整自动化 1,212 项通过，用时 71.08 秒；Ruff 通过。
- 合并前独立审查修复 3 个集成边界后，最终完整自动化为 1,215 项通过，用时 183.59 秒；Ruff 通过。
- 七轮合成基准输出 `TASK_CENTER_BENCHMARK_OK`，所有中位数和事件循环间隙均低于门禁。
- 所有响应测试仅使用临时目录和固定合成数据，不读取真实 Telegram 或下载数据。

## 第 1 轮：源码、测试、静态检查与七轮基准

状态：通过。

执行命令：

```powershell
$env:QT_QPA_PLATFORM='offscreen'
.\.venv\Scripts\python.exe -m pytest tests/test_repository.py tests/test_task_center.py tests/test_task_refresh.py tests/test_controller.py tests/ui/test_main_window.py tests/ui/test_task_center_responsiveness.py -q
.\scripts\test.ps1
.\.venv\Scripts\python.exe scripts\benchmark_task_center.py --repeats 7
git diff --check
```

- 重点回归：226 项通过，23.43 秒。
- 全套自动化：1,212 项通过，71.08 秒；Ruff 通过。
- `git diff --check`：通过。
- 合成规模：10,000 个任务、50,000 个媒体、50 ms 慢仓储、500 次连续进度事件。
- 模型与批处理：初载后模型重置 0 次；500 次事件形成 1 个 dirty 批次。
- 事件循环最大间隙：11.00 ms。

七轮原始值（顺序为任务初载、筛选、单行更新、媒体首屏，单位 ms）：

```text
RUN_1=30.73,12.76,29.30,23.89
RUN_2=29.63,9.57,36.27,22.76
RUN_3=35.38,5.43,38.05,22.44
RUN_4=35.31,4.94,41.60,22.58
RUN_5=38.04,5.86,47.07,38.19
RUN_6=30.49,5.70,40.93,22.26
RUN_7=32.02,5.31,66.45,19.07
```

中位数：任务初载 32.02 ms、筛选 5.70 ms、单行更新 40.93 ms、媒体首屏 22.58 ms。

## 第 2 轮：便携包、隐私与冻结 GUI

状态：通过。

- 构建前按绝对路径确认工作树内冻结程序进程数为 0。
- `scripts\build.ps1` 成功；其内置全套自动化为 1,212 项通过（101.93 秒），Ruff 通过，并输出 `PACKAGED_SMOKE_OK`。
- `scripts\smoke.ps1` 独立复跑并输出 `PACKAGED_SMOKE_OK`。
- 便携 ZIP 共 195 个条目；`data/`、`downloads/`、日志、数据库、凭据、`.part` 和 `.corrupt*` 隐私扫描命中 0。
- ZIP 解压到工作树内独立目录后，冻结 EXE `--self-test` 退出码为 0，报告 `ok=true`。
- 冻结构建后再次运行合成 Qt 响应门禁：1 项通过，9.18 秒；没有向冻结程序注入业务数据库。

制品：

- `TelegramDownloader-0.18.4-win-x64-portable.zip`：66,053,704 字节；SHA-256 `1CAFFA9A705E7FFA1893DEBFB4E771D775F1F090FD5D03CFA9B2E1AEF39CC365`。
- `TelegramDownloader.exe`：6,154,718 字节；SHA-256 `5A4CBFC03CE2C2256E36D547818FFC30AFABE26CC02986B8399A861D27D2DB98`。

## 第 3 轮：安装器、保留语义与人工 GUI

状态：通过。

- `scripts\build-installer.ps1 -SkipAppBuild` 成功并输出 `INSTALLER_SMOKE_OK`。
- 安装器拒绝 C 盘目标；在工作树内完成首次安装、冻结程序自检、带合成数据库与缩略图的覆盖升级、正常卸载。
- 覆盖升级与正常卸载前后，合成数据库、缩略图和保留标记的 SHA-256 均不变；卸载后受管 EXE 已移除，保留标记仍存在。
- 冻结 GUI 可启动且版本显示为 0.18.4；首次登录 API 指南可关闭，任务筛选输入 `synthetic-smoke` 后正确显示空筛选结果。
- 设置页可切换到“关于与更新”，显示“仅在点击后检查正式版更新”和“检查更新”按钮；未触发外网检查。
- 第二次启动时目标窗口仍只有 1 个；关闭主窗口后可见窗口数为 0，再次启动恢复同一窗口句柄 `59573816`，单实例与托盘后台行为成立。
- 干净冻结库没有媒体，未伪造人工媒体滚动或选择结论；50,000 媒体首屏、追加分页、可见/已选媒体更新由隔离合成 Qt 门禁覆盖。
- GUI 检查结束后，仅按已核对的冻结 EXE 绝对路径停止 1 个测试进程，停止后同路径进程数为 0。

制品：

- `TelegramDownloader-0.18.4-win-x64-setup.exe`：48,269,936 字节；SHA-256 `8F3DD3C88FA33D728DFF351B01C79C81E604F048929590ABD6A617CF2237AD6B`。

## 提交前最终门禁

状态：通过。

- `git status --short`：只有本验证记录待提交。
- `git diff --check`：通过。
- `scripts\test.ps1`：1,212 项通过，82.04 秒；Ruff 通过。

## 合并前独立审查与修复

状态：通过。

- 独立全范围代码审查未发现 Critical 问题，发现并修复 3 个 Important 集成边界：普通任务补丁不再重复发出未变化的选择信号并重载媒体第一页；已登录启动恢复的待调度任务读取移到后台线程；任务中心隐藏后周期进度不再查询任务或可见媒体。
- 3 个最小回归测试先在旧实现上按预期失败，再在修复后通过；相关窗口、控制器、协调器和 10,000/50,000 合成 Qt 响应回归共 192 项通过，用时 22.70 秒。
- 修复后完整自动化 1,215 项通过，用时 183.59 秒；Ruff 输出 `All checks passed!`。
- 初次复跑纯 asyncio 合成心跳时，Windows 5 ms 睡眠偶尔跨越两个系统计时 tick，产生 26–27 ms 的环境性误报；GC 回调确认事件循环测量期间没有垃圾回收停顿，真实 5 ms Qt 心跳门禁持续通过。基准改为逐次让步并直接测量相邻事件循环轮次间隙，仍保留 20 ms 门槛。
- 修正后的七轮基准连续执行 5 次均输出 `TASK_CENTER_BENCHMARK_OK`；事件循环最大间隙每次均为 16.00 ms，任务初载、筛选、单行更新和媒体首屏中位数范围分别为 15.08–34.01 ms、4.08–7.87 ms、21.19–34.72 ms、11.51–22.19 ms。

## 主分支合并后门禁

状态：通过。

- `main` 快进到审查修复提交后，首次全套门禁有 1 个既有激活子进程测试因 3 秒冷启动上限超时，其余 1,214 项通过；该测试随后隔离连续执行 10 次均通过，单次为 0.44–0.84 秒。测试进程外层等待调整为 10 秒，生产激活协议的 1 秒超时保持不变。
- 第二次全套门禁有 1 个固定刷新窗口测试在 Windows 计时 tick 下测得 62 ms，超过测试写死的 60 ms，其余 1,214 项通过；功能结果仍为单一 dirty 批次。该测试改为在持续追加进度时等待实际首批应用，直接断言截止时间不会被后续事件延长，并隔离连续执行 20 次全部通过。
- 最终 `scripts\test.ps1` 为 1,215 项通过，用时 92.72 秒；Ruff 输出 `All checks passed!`，主分支工作树没有生产代码补丁待提交。

## 隐私与发布边界

- 禁止把账号、会话、消息、搜索词、用户文件名、用户下载路径、凭据或真实数据库写入验证证据。
- 远端标签、上传和在线更新指针不在本地候选验收范围内。
