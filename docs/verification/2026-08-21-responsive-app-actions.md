# 响应式应用操作验证记录

验证日期：2026-08-22（Asia/Shanghai）

验证分支：`codex/performance-optimization`

验证源码提交：`474bdc6`

## 自动化质量门禁

- [x] `scripts/test.ps1`
  - 最终审阅后结果：`739 passed in 101.44s`。
  - Ruff 结果：`All checks passed!`。
- [x] 控制器与订阅控制器定向回归
  - 结果：`106 passed in 20.31s`；Ruff 通过。
  - 扫描建任务、内容入队、订阅建任务和完整性操作完成后的任务刷新均不再走事件循环内同步读取。
- [x] 下载压力、账号范围搜索、任务管理、控制器、应用动作和主要 Qt 页面定向回归
  - 结果：`198 passed in 31.56s`。
  - 最慢用例为 50 任务队列压力测试，调用阶段耗时 `23.43s`、准备阶段耗时 `1.27s`；其后最慢用例为应用初始化，耗时 `0.52s`。
- [x] 验证前 `git status --short`
  - 结果：工作树无未提交变更。

## 响应性与一致性证据

- [x] 缓冲下载慢磁盘用例持续记录事件循环心跳，证明文件写入等待未阻塞 GUI 事件循环。
- [x] 任务快照从工作线程读取，并发刷新被合并；人工阻塞读取期间事件循环心跳保持运行。
- [x] 内容历史删除/清空、缩略图缓存统计/清理、诊断历史/保存/导出和设置持久化等 8 类阻塞操作均有事件循环心跳覆盖。
- [x] 暂停、继续、重试、优先级、归档和恢复动作按任务集合批量执行，每次动作只做一次批量查询、一次批量写入和一次界面刷新。
- [x] 任务与订阅数据在 ID 顺序不变时使用增量更新，不触发 `modelReset`；选择状态与滚动位置可保留。
- [x] 搜索、加载更多等可替换动作采用 `REPLACE_LATEST`；刷新、登录、诊断、保存和清缓存等不可重入动作采用 `DEDUPLICATE`，所有应用动作键均有显式策略。

## Windows 图形界面冒烟

- [x] 从源码启动应用，更新提示与未登录提示可以正常关闭，主窗口保持可交互。
- [x] 任务中心、设置和健康诊断页面即时打开；设置页正确显示缩略图缓存大小，诊断页按钮状态正确。
- [x] 账号内容与自动订阅页面即时呈现离线数据和未登录状态，并进入登录引导；关闭引导后页面继续可操作。
- [x] 应用通过窗口关闭按钮正常退出，无遗留应用窗口。
- [ ] 未在实机中执行真实账号授权、云端搜索、下载、订阅运行或清缓存操作。
  - 这些流程需要账号或会修改外部/本地数据，本次以对应的自动化端到端、集成和心跳测试作为验证证据，不将其误记为人工通过。

本记录只包含合成测试结论和本地提交标识，不包含 Telegram 标识、真实文件名、凭据、消息、搜索词或用户绝对路径。
