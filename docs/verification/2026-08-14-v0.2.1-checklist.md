# TelegramDownloader v0.2.1 验证清单

验证日期：2026-08-14

发布版本：`0.2.1`

## 发布状态

- GitHub 正式发布：<https://github.com/lx3559359/TelegramDownloader/releases/tag/v0.2.1>
- 魔塔镜像仓库：<https://modelscope.cn/models/lx3559359/TelegramDownloader>
- GitHub Release 为正式版：`isDraft=false`、`isPrerelease=false`，包含预期的 7 个附件。
- GitHub `main`、魔塔 `source`、本地 `v0.2.1` 标签和发布提交均为 `61b52cf7f8ef0104312510e3e3252e705305c7d4`。
- 双端 `latest.json` 均指向 `0.2.1`，正式发布事务退出码为 0。

## 二维码修复

- 二维码渲染根据矩阵模块数选择不超过 300 像素的整数模块尺寸，保留四模块白色静区。
- 登录窗口二维码视口固定为 `300 × 300`，禁止插值拉伸，并在切页后重新计算窗口尺寸。
- 43 字符令牌的视觉证明尺寸：窗口 `520 × 579`、视口 `300 × 300`、二维码 `287 × 287`，四边完整可见。
- 新增短、中、长令牌、静区、极小视口和真实 Qt 布局回归测试。

## 测试与打包

- 完整源码测试：`187 passed`。
- Ruff：`All checks passed!`。
- 正式发布脚本重新构建便携包与安装包，并通过打包运行时和安装器冒烟测试。
- 打包程序 `--self-test`：`ok=true`、`version=0.2.1`；PySide6、Telethon、qasync、qrcode、SQLite、DPAPI 均为 `true`。
- 自检报告的设置、密钥、数据库、日志、缓存、临时目录、下载目录和更新目录全部位于 `D:\Codex Project\Telegram下载器\dist\TelegramDownloader` 下。

## 数据保全

- 发布前在项目内 `.local/backups/pre-v0.2.1-20260814-111214` 备份运行数据，共 6 个文件、34,229 字节，并逐文件校验。
- 两次正式构建后，配置、DPAPI 密钥、任务数据库和下载数据与备份一致。
- 验证过程中生成的 `data/logs/self-test.json` 已恢复，最终 6 个文件与发布前备份逐文件 SHA-256 一致。

## 在线更新验证

- 使用应用真实 `UpdateCoordinator` 从已安装 `0.2.0` 的视角联网检查。
- 结果：`blocked=false`、`version=0.2.1`、`available_sources=[modelscope, github]`、签名清单有效。
- 正式发布脚本从 GitHub 与魔塔下载候选资产并逐字节比较后，才推进双方稳定版指针。

## 发布资产

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `TelegramDownloader-0.2.1-source.zip` | 204,182 | `523d245c8d49758613deb0eb1782d698510c7b84e35a448037e37ccced7701bf` |
| `TelegramDownloader-0.2.1-win-x64-portable.zip` | 65,449,320 | `c736fdfa54d2849e2f276f63c25eea14fd7f8059518a83af125a3b6cf9bb0830` |
| `TelegramDownloader-0.2.1-win-x64-setup.exe` | 47,658,013 | `f186cfb584877105748e6f3570f4d5ccddceb382c6d43ac9b16fac99f31f2531` |
| `update-manifest.json` | 2,500 | `1ebf46575619f476e0c2f680e01f50a12a933e329a5aa702691c46916b7be923` |
| `update-manifest.sig` | 89 | `e6a845489ecc2e3252f5158e99e8d4be86b2abdbd3d3e1a69fba46fef31fdbf0` |
| `latest.json` | 57 | `b399c543fd11423662ad24df1ecff113fa10e4edb0f37d53b6bdf3cdc19ea976` |

`v0.2.1` 满足二维码完整显示、项目路径内数据存储、便携包与安装包、签名双源在线更新以及 GitHub/魔塔同步发布要求。
