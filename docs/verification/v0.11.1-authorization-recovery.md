# TelegramDownloader v0.11.1 发布验收记录

发布日期：2026-08-16  
发布范围：Telegram 授权失效统一恢复与脱敏诊断

## 本地门禁

- 功能实现按 TDD 完成，覆盖网关授权原因映射、已连接但未授权、订阅持久化后回调、并发幂等恢复、网络故障保留会话、登录后重置和生产装配。
- 合并前与合并后完整测试均为 `650 passed`，Ruff 为 `All checks passed!`。
- Windows 便携构建和打包自检输出 `PACKAGED_SMOKE_OK`。
- 发布包隐私边界要求为零用户数据库、设置、DPAPI 凭据、Telegram 会话、日志、下载文件和诊断包。

## 公开发布证据

正式发布事务完成后补充源码提交、远端标签、资产大小与 SHA-256、双源稳定指针及在线更新发现结果；发布标签不会因证据提交而移动。
