# 渐进式媒体搜索验证记录

验证日期：2026-08-21（Asia/Shanghai）  
验证分支：`codex/performance-optimization`  
验证源码提交：`efa59221405c4230d7003c7f2f6a3f05967d6e6c`

## 自动化质量门禁

- [x] 渐进搜索、目录、网关、控制器、账号范围搜索和 Qt 内容界面测试
  - 结果：`208 passed in 18.46s`。
  - 命令包含 `--durations=10`；最慢缩略图并发限制用例为 `3.26s`，相册并发限制用例为 `2.72s`。
- [x] `python -m ruff check src tests`
  - 结果：`All checks passed!`。
- [x] 验证前 `git status --short`
  - 结果：工作树无未提交变更。

## 渐进呈现与响应性证据

- [x] 当相册扩展被阻塞时，直接命中批次已先到达；解除阻塞后再收到包含完整相册的稳定批次。
- [x] 同一媒体在临时批次与稳定批次中保持相同结果 ID，搜索代次不匹配或已被替换的控制器任务不会覆盖当前结果。
- [x] 单页目录提交测试确认结果写入、结果计数、游标/完成状态和稳定结果读取只打开一个 SQLite 连接，并返回一致的会话与结果状态。
- [x] Qt 结果模型的插入、更新、删除均未触发 `modelReset`；稳定批次到达前，加入下载队列动作保持禁用。
- [x] 相同缓存键的并发缩略图请求只调用一次网关；空结果或非取消异常进入 30 秒冷却，立即重试不会重复请求。
- [x] 缩略图缓存写入在线程中执行，可见行继续按需请求，历史清理和账号隔离回归保持通过。

本记录只包含合成测试结论和本地提交标识，不包含真实 Telegram 账号、会话、关键词、消息、媒体标识或用户绝对路径。
