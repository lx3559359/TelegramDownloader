# TelegramDownloader v0.4.2 三轮真实化自检记录

日期：2026-08-15

分支：`codex/three-pass-qa-v042`

状态：执行中；仅本地验证，不推送或发布

## 数据安全基线

- 运行根目录：`D:\Codex Project\Telegram下载器\.worktrees\content-ux\dist\TelegramDownloader`。
- 基线运行数据：26 个文件，共 10,181,191 字节，路径/大小/哈希聚合 SHA-256 为 `D1A4F1E4D5CF8D49271C8DA65EBF9080C048EFD4E626EDF2FBEDBE357960ED07`。
- 指纹只读取文件路径、大小和哈希，不读取或记录凭据、会话正文、群组名称、关键词或消息内容。

## 第一轮：正常路径与独立恢复

| 检查项 | 结果 | 直接证据 |
| --- | --- | --- |
| 自动化与静态检查 | 通过 | 350 项测试通过；Ruff 无错误 |
| 已保存会话启动 | 通过 | 会话恢复成功；登录窗口未出现 |
| 群组同步与刷新 | 通过 | 缓存与刷新后均为 4 项；开始反馈和完成状态可见 |
| 搜索与进度 | 通过 | 搜索完成，得到 15 项；搜索期间进度和取消入口可见 |
| 缩略图与双击预览 | 通过 | 双击后应用内预览窗口成功打开并关闭 |
| 选择与入队状态 | 通过 | 15 项均已入队时批量按钮禁用，选择数为 0，排除计数可见 |
| 真实下载与暂停 | 通过 | 下载新增 524,288 字节；无媒体错误；任务恢复为暂停 |
| 受控崩溃 | 通过 | 专用进程按预期退出；崩溃前再次新增 524,288 字节，状态为下载中 |
| 重启恢复与续传 | 通过 | 仓库恢复为排队，3 项恢复可调度，自动续传且字节未倒退 |
| 安全关闭 | 通过 | 15 项最终暂停；关闭 11.8 秒；网关断开；借用发送器、活动调度任务和待处理异步任务均为 0 |
| GitHub/魔塔在线更新 | 通过 | 两源签名状态均有效、远端稳定版为 0.4.0；本地 0.4.2 返回无更新且不阻断 |
| 安装、升级、卸载保留 | 通过 | `INSTALLER_SMOKE_OK`；C 盘目标拒绝，项目内安装、覆盖和卸载数据保留通过 |

## 第二轮：异常与重复交互

| 检查项 | 结果 | 直接证据 |
| --- | --- | --- |
| 第二次会话启动 | 通过 | 已保存会话恢复，登录窗口未出现 |
| 第二次群组同步、搜索和预览 | 通过 | 刷新前后均为 4 项；搜索 15 项；进度可见；双击预览成功 |
| 重复/异步交互状态 | 通过 | 异步动作及内容页 20 项焦点测试通过；真实刷新、搜索按钮均即时进入忙碌并恢复；退出后无活动进程 |
| 已入队结果反馈 | 通过 | 15 项均已入队，批量按钮保持禁用且选择数为 0；隔离 UI 回归覆盖可选结果的取消、确认和重复入队路径 |
| 第二次受控崩溃 | 通过 | 专用进程退出前状态为下载中并新增 524,288 字节 |
| 第二次重启恢复 | 通过 | 仓库恢复为排队，3 项恢复可调度并自动续传，字节未倒退 |
| 第二次安全关闭 | 通过 | 1 项完成、14 项暂停；关闭 14.72 秒；网关断开；借用发送器、活动调度任务和待处理异步任务均为 0 |
| 更新异常策略 | 通过 | 39 项更新及对话框测试通过，覆盖单源降级、签名/双源矛盾阻断、旧版本无更新和最低更新器策略 |
| 第二次真实在线更新 | 通过 | GitHub 与魔塔均为签名有效的 0.4.0，本地 0.4.2 无更新且不阻断 |
| 第二次安装、升级、卸载保留 | 通过 | `INSTALLER_SMOKE_OK` |

## 第三轮：最终候选与重建

尚未执行。

## 发布边界

- 不推送 GitHub 或魔塔。
- 不创建 tag、Release 或远端更新清单。
- 完成三轮和最终验证后仅在本地快进合并至 `main`。
