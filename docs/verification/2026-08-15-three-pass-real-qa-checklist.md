# TelegramDownloader v0.4.2 三轮真实化自检记录

日期：2026-08-15

分支：`codex/three-pass-qa-v042`

状态：三轮验证完成；交付边界为本地快进合并，不推送或发布

## 数据安全基线

- 运行根目录：`D:\Codex Project\Telegram下载器\.worktrees\content-ux\dist\TelegramDownloader`。
- 基线运行数据：26 个文件，共 10,181,191 字节，路径/大小/哈希聚合 SHA-256 为 `D1A4F1E4D5CF8D49271C8DA65EBF9080C048EFD4E626EDF2FBEDBE357960ED07`。
- 指纹只读取文件路径、大小和哈希，不读取或记录凭据、会话正文、群组名称、关键词或消息内容。

## 第一轮：正常路径与独立恢复

| 检查项 | 结果 | 直接证据 |
| --- | --- | --- |
| 自动化与静态检查 | 通过 | 350 项测试通过；Ruff 无错误 |
| 已保存会话启动 | 通过 | 会话恢复成功；登录窗口未出现 |
| 群组同步与刷新 | 通过 | 缓存与刷新后均为 4 项；开始反馈和完成状态可见 |
| 搜索与进度 | 通过 | 搜索完成，得到 15 项；搜索期间进度和取消入口可见 |
| 缩略图与双击预览 | 通过 | 双击后应用内预览窗口成功打开并关闭 |
| 选择与入队状态 | 通过 | 15 项均已入队时批量按钮禁用，选择数为 0，排除计数可见 |
| 真实下载与暂停 | 通过 | 下载新增 524,288 字节；无媒体错误；任务恢复为暂停 |
| 受控崩溃 | 通过 | 专用进程按预期退出；崩溃前再次新增 524,288 字节，状态为下载中 |
| 重启恢复与续传 | 通过 | 仓库恢复为排队，3 项恢复可调度，自动续传且字节未倒退 |
| 安全关闭 | 通过 | 15 项最终暂停；关闭 11.8 秒；网关断开；借用发送器、活动调度任务和待处理异步任务均为 0 |
| GitHub/魔塔在线更新 | 通过 | 两源签名状态均有效、远端稳定版为 0.4.0；本地 0.4.2 返回无更新且不阻断 |
| 安装、升级、卸载保留 | 通过 | `INSTALLER_SMOKE_OK`；C 盘目标拒绝，项目内安装、覆盖和卸载数据保留通过 |

## 第二轮：异常与重复交互

| 检查项 | 结果 | 直接证据 |
| --- | --- | --- |
| 第二次会话启动 | 通过 | 已保存会话恢复，登录窗口未出现 |
| 第二次群组同步、搜索和预览 | 通过 | 刷新前后均为 4 项；搜索 15 项；进度可见；双击预览成功 |
| 重复/异步交互状态 | 通过 | 异步动作及内容页 20 项焦点测试通过；真实压力探针确认重复刷新被合并、按钮恢复、取消搜索后表单恢复、替代搜索完成 6 项且结束时活动动作数为 0 |
| 单实例保护 | 通过（专用测试） | Windows 实例锁及异步桥 5 项焦点测试通过；最终 EXE 真实启动时窗口列表仅出现一个目标窗口 |
| 已入队结果反馈 | 通过 | 15 项均已入队，批量按钮保持禁用且选择数为 0；隔离 UI 回归覆盖可选结果的取消、确认和重复入队路径 |
| 第二次受控崩溃 | 通过 | 专用进程退出前状态为下载中并新增 524,288 字节 |
| 第二次重启恢复 | 通过 | 仓库恢复为排队，3 项恢复可调度并自动续传，字节未倒退 |
| 第二次安全关闭 | 通过 | 1 项完成、14 项暂停；关闭 14.72 秒；网关断开；借用发送器、活动调度任务和待处理异步任务均为 0 |
| 更新异常策略 | 通过 | 39 项更新及对话框测试通过，覆盖单源降级、签名/双源矛盾阻断、旧版本无更新和最低更新器策略 |
| 第二次真实在线更新 | 通过 | GitHub 与魔塔均为签名有效的 0.4.0，本地 0.4.2 无更新且不阻断 |
| 第二次安装、升级、卸载保留 | 通过 | `INSTALLER_SMOKE_OK` |

## 第三轮：最终候选与重建

| 检查项 | 结果 | 直接证据 |
| --- | --- | --- |
| 第三次会话启动 | 通过 | 已保存会话恢复，登录窗口未出现 |
| 第三次群组同步、搜索和预览 | 通过 | 刷新前后均为 4 项；搜索 15 项；进度可见；双击预览成功 |
| 第三次选择与队列状态 | 通过 | 15 项均已入队，选择数为 0，批量按钮正确禁用；完整 UI 回归覆盖未入队结果的选择、取消、确认和去重 |
| 第三次真实下载与暂停 | 通过 | 下载新增 524,288 字节；无媒体错误；任务恢复为暂停 |
| 第三次受控崩溃 | 通过 | 专用进程退出前状态为下载中并新增 524,288 字节 |
| 第三次重启恢复 | 通过 | 仓库恢复为排队，3 项恢复可调度并自动续传，字节未倒退 |
| 第三次安全关闭 | 通过 | 1 项完成、14 项暂停；关闭 12.88 秒；网关断开；借用发送器、活动调度任务和待处理异步任务均为 0 |
| 第三次真实在线更新 | 通过 | GitHub 与魔塔均为签名有效的 0.4.0，本地 0.4.2 无更新且不阻断 |
| 完整自动化和静态检查 | 通过 | 构建内 350 项测试通过、Ruff 通过；构建后再次 350 项测试通过、Ruff 通过 |
| 便携版构建 | 通过 | `PACKAGED_SMOKE_OK`；打包自检退出码 0、版本 0.4.2 |
| 安装包构建与保留 | 通过 | Inno Setup 编译成功；`INSTALLER_SMOKE_OK` |
| 构建数据保留 | 通过 | 构建前后均为 27 个文件、20,242,433 字节，聚合 SHA-256 均为 `811C333235FB55E8B25A10BAFF6B57B4248E3157EBA80436EFCEC82A46F3EE6D` |
| 便携包隐私 | 通过 | ZIP 共 192 个条目，`data/` 与 `downloads/` 条目为 0 |
| 最终 EXE 启动 | 通过（环境限制已记录） | 打包 EXE 启动后被窗口系统识别为唯一标题“Telegram 下载器”的窗口；Codex 图形输入被 Windows 拒绝，测试 PID 按精确路径清理；三轮 Qt 正常关闭与三轮网关/调度器收尾均通过 |
| 残留进程 | 通过 | 验证结束后 `TelegramDownloader.exe` 进程数为 0 |

## 最终候选产物

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `dist/TelegramDownloader/TelegramDownloader.exe` | 5,708,505 | `1D9288C2891C420E83413E0B26753FE5EF7523E036F6BFCD2D8979503F480FB9` |
| `dist/TelegramDownloader-0.4.2-win-x64-portable.zip` | 65,562,258 | `D60FED849B0364B488E4FD1F9EA3750EC134365C11F3AA7CA6DE907D2E4A67FA` |
| `dist/release/TelegramDownloader-0.4.2-win-x64-setup.exe` | 47,762,823 | `B591C39C783F9CA909DBFAFC53B4F77CA5F4107D52DF21E53052DABDEE42228C` |

## 缺陷与修复结论

- 三轮均未发现可稳定复现的生产缺陷，因此未进行猜测性生产代码修改，也没有伪造 TDD 的 RED 阶段。
- 新增压力探针首次等待刷新活动键时出现 50ms 采样竞态；状态采样证明应用已正确进入忙碌，探针在事件循环调度前开始等待。该项目内临时探针加入一个事件循环 tick 后连续通过，未修改生产代码。
- Windows 图形输入权限错误来自 Codex Computer Use 运行环境；下载器窗口已真实创建，源代码 Qt 旅程、打包自检和安装器自检均未复现应用错误。
- 本轮新增的是三轮验证设计、实施计划、隐私安全的在线更新探针和本验证记录；临时探针与截图保留在被 Git 忽略的项目目录内。

## 发布边界

- 不推送 GitHub 或魔塔。
- 不创建 tag、Release 或远端更新清单。
- 完成三轮和最终验证后仅在本地快进合并至 `main`。
