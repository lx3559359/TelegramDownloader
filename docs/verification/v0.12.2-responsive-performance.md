# TelegramDownloader v0.12.2 响应性能验收记录

## 发布范围

- 基线：已公开的 `v0.12.1` 搜索限流恢复版本。
- 下载：后台批量写盘、确认字节进度、暂停/取消/完成持久化边界。
- 搜索：直接命中渐进呈现、相册稳定批次、事务提交、缩略图请求合并。
- 交互：任务批量命令、数据库与文件操作异步化、模型增量刷新、动作去重与替换策略。
- 兼容性：数据库 schema 保持 4，在线更新协议和应用数据目录不变。

## 合并与自动化验证

本次先同步 GitHub `v0.12.1` 主线，再合并性能优化分支。搜索模块冲突解决同时保留 `v0.12.1` 的 FloodWait 倒计时、同游标重试与未完成搜索恢复，以及 `v0.12.2` 的渐进结果批次和事务化稳定提交。

| 检查 | 结果 |
| --- | --- |
| 搜索服务、控制器、账号内容 UI 与全账号端到端专项 | `155 passed in 19.35s` |
| 合并后完整 pytest 套件 | `747 passed in 133.41s` |
| 版本提升后完整 pytest 套件 | `747 passed in 52.75s` |
| 版本与打包契约 | `7 passed in 0.08s` |
| Ruff | `All checks passed!` |
| 冲突标记与差异格式检查 | 通过 |

自动化覆盖慢磁盘事件循环心跳、8 MiB 批量写入、断点与取消持久化、50 任务压力队列、直接结果先于相册、稳定前禁止入队、搜索分页事务、缩略图并发合并、任务批量状态写入、阻塞操作工作线程边界、Qt 模型无重置更新以及应用动作策略。

## 图形界面冒烟

- Windows 源码应用可正常启动与退出，任务中心、设置、健康诊断、账号内容和自动订阅页面可即时切换。
- 未登录状态正确进入登录引导，关闭引导后离线页面继续可操作。
- 本轮未在实机中执行真实账号授权、云端搜索、下载或订阅运行；这些路径以自动化端到端、集成与事件循环心跳测试作为发布前证据。

## 正式发布

正式发布提交为 `cd734aefb4c126a2db516ea675207f04a3af1bf2`。GitHub 与魔搭的 `v0.12.2` annotated tag 剥离后均指向该提交；发布时 GitHub `main` 与魔搭 `source` 也均指向该提交。

`scripts/release/release.ps1 -Version 0.12.2` 以退出码 0 返回 `RELEASE_PUBLISHED v0.12.2`。发布事务内完整套件为 `747 passed in 60.34s`，Ruff 返回 `All checks passed!`。冻结程序构建返回 `PACKAGED_SMOKE_OK`，安装器构建、非系统盘安装、升级、卸载数据保留和冻结自检返回 `INSTALLER_SMOKE_OK`。

| 产物 | 字节 | SHA-256 |
| --- | ---: | --- |
| `TelegramDownloader-0.12.2-source.zip` | 853,538 | `d23b9d7c4f68e9a19008ca7337dbae86265bde685e73f44949823c0ea7d2b5ad` |
| `TelegramDownloader-0.12.2-win-x64-portable.zip` | 67,350,496 | `8b95113825f483c6a3c99c7defcaddd129b7ca402a0c3d4ff6728ad6595cef27` |
| `TelegramDownloader-0.12.2-win-x64-setup.exe` | 47,968,472 | `5804246ea24771bf7e651f327c18e559d36cf6b0af7f396726feba9a02c358a2` |
| `update-manifest.json` | 4,938 | `017da02e9da741748933f0ca0cefe96f9a909d24a522def41b7ec27ea0366073` |
| `update-manifest.sig` | 89 | `2617b40c2c1e126fc8eb90b7858d811bf8f9d29943a044a37c17129e2c5e9f0a` |

独立发布后复核结果：

- GitHub Release 已公开，`isDraft=false`、`isPrerelease=false`，包含 7 个资产；资产名称、字节数和 GitHub 记录的 SHA-256 与本地候选一致。
- GitHub `main`、魔搭 `source` 以及两端 `v0.12.2` 标签剥离目标均确认发布提交不变。
- 从 GitHub 和魔搭重新下载的正式资产与本地候选逐字节一致；两端 `latest.json` 均指向 0.12.2，本地与 GitHub 正式指针为 58 字节、SHA-256 `dc6b483b95e9c1cee88c46a7b3ac65ed69e150a85363f103df24c99a7c59c5cd`。
- 使用内置可信公钥重新验证 Ed25519 清单签名成功，版本为 0.12.2，密钥标识为 `release-2026-01`；运行包和安装包再次按清单验证大小与 SHA-256。
- 冻结程序重新执行 `--self-test`：`ok=true`、版本 0.12.2、catalog schema 4，PySide6、Telethon、qasync、qrcode、SQLite 和 DPAPI 均可用，13 个写入路径全部位于冻结程序根目录。
- 以 0.12.1 为当前版本执行真实 `UpdateCoordinator` 双源检查：GitHub 与魔搭均为 `valid`，发现版本 0.12.2，`blocked=false`。

- GitHub：https://github.com/lx3559359/TelegramDownloader/releases/tag/v0.12.2
- 魔搭：https://modelscope.cn/models/lx3559359/TelegramDownloader

本记录不包含账号、群组、消息、搜索词、凭据、真实文件名、用户绝对路径或未脱敏日志。
