# v0.10.0 图形化健康诊断验证

## 验证范围

- 诊断领域模型、固定状态归约与取消语义。
- 项目路径、磁盘、组件、任务库、内容库、凭据、Telegram 和双源更新探针。
- 原子历史保存、严格隐私白名单及双条目 ZIP。
- 图形页面、控制器接线、关闭收敛和 Windows 两种交付形式。
- v0.9.0 直启数据继承、非 C 盘边界、GitHub/魔搭签名发布与在线发现。

## 发布前门禁

结构化代码审查、三轮完整测试、复制数据真人化操作、冻结程序自检、便携包/安装包检查和隐私扫描将在发布代码固定后记录。

### 结构化规格与代码审查

- 审查基线：`63042ff7fc3cd1185c217dbf15f47a8d406af7ac`。
- 首轮功能头：`5a5e80c73faa4bc2e89b97ca30bff610432c8e60`。
- 审查修复头：`fa5a86f`。
- 已解决重要项：SQLite 普通只读连接产生 WAL/SHM 副作用；诊断字段缺少逐检查项键、类型和固定说明白名单；ZIP 原子移动前未显式 `fsync`；状态缺少颜色辅助、历史完成时间和百分比；取消本地线程探针时未等待真实收敛；结构异常的更新源结果可能误判为正常。
- 每项均先建立失败回归再修复；修复后完整门禁为 580 项通过、Ruff 通过。
- 未发现未解决的 Critical 或 Important 问题。

### 三轮固定提交自动化门禁

- 固定提交：`3fe67ed9ed40feadf7901f0180dc186c4169187a`；三轮开始前工作树均为干净状态。
- 第 1 轮：580 项测试通过、Ruff 通过，耗时 16.239 秒。
- 第 2 轮：580 项测试通过、Ruff 通过，耗时 15.394 秒。
- 第 3 轮：580 项测试通过、Ruff 通过，耗时 16.382 秒。
- 三轮退出码均为 0，且均在同一提交上执行。

### v0.9.0 数据副本真人化诊断

- 仅将 v0.9.0 直启目录中的 `data` 与 `downloads` 复制到工作树 `.build-temp` 的 F 盘隔离目录；原目录 8 个文件复制前后逐文件 SHA-256 一致。
- 诊断读取到 1 个任务、2 个媒体项、1 个账号、4 个群组/频道、1 次搜索、0 个搜索结果、0 个订阅和 0 次订阅运行；与只读 SQL 聚合计数一致。
- 9 个检查项全部通过；导出 ZIP 只有 `diagnostic-report.json` 和 `diagnostic-summary.txt`，原始数据库私密字段、解密凭据、加密凭据块和两处绝对路径扫描均无命中。
- 诊断前后，隔离副本中排除新建 `data/diagnostics` 后的所有原文件名称和哈希不变；v0.9.0 原目录仍完全不变。
- 真实运行暴露并修复了报告微秒时间在落盘时被截断、导致保存后严格回读不相等的问题；新增回归后完整门禁为 581 项通过、Ruff 通过。

## 发布证据

正式资产 URL、大小、SHA-256、发布标签提交、双源字节一致性、直启数据哈希和最终测试计数将在发布完成后追加；发布前不预填公开证据。
