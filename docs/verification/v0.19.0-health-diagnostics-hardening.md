# TG 快取 v0.19.0 健康诊断加固验证

验证日期：2026-08-31

合并目标：main

实现提交范围：9e66340..eb829d1

## 当前结论

- 健康诊断加固分支已无冲突快进合并到 main，合并后的功能提交为 eb829d18ba2cc550894115549480a9a4656d86e8。
- 合并前完整自动化为 1,327 项通过，用时 152.92 秒；Ruff 通过。
- 合并后完整自动化为 1,327 项通过，用时 143.83 秒；Ruff 通过。
- 独立审查已覆盖命令单实例、业务状态只读、WAL 只读快照、完整 schema、外键、枚举域、凭据、双存储卷、网络超时、取消收敛、脱敏导出和受控构建 PATH；所有 Critical/Important 发现均已解决。
- 用户已明确授权合并并发布 v0.19.0；正式发布结果将在发布事务结束后补录。

## 命令与业务状态

- --self-test 的真实仓储回归验证任务、媒体、搜索和订阅记录及更新时间前后不变。
- --self-test、--update-health-check 在进入运行时自检前获取单实例锁；锁被占用时返回退出码 2，且业务数据库访问桩为零调用。
- GUI 正常启动的中断恢复合同保持不变。

## 数据库、凭据和存储

- WAL 测试保持写连接开启并提交未 checkpoint 的记录；只读诊断能够在同一事务快照中看到最新提交。
- 任务库和内容库逐表校验完整列集合、SQL 类型、非空与默认值、外键定义和所有持久化业务枚举；损坏样例均映射为固定脱敏结果。
- 长值与类型异常在进入枚举集合前由有界查询拒绝，避免无界载入。
- 凭据测试仅用合成设置与密文，覆盖缺失、不可解密、不完整和完整配置。
- 存储测试覆盖当前下载目录可写性、同卷去重、双卷低空间和不可用情况，报告不含盘符或路径。

## 网络、取消和详情

- Telegram 与双更新源探针通过可注入短时限验证超时映射和任务取消；Telegram 诊断不会调用共享连接恢复器。
- 控制器与 Qt 页面测试验证“正在取消”即时反馈、重复取消禁用和完成后状态复位。
- 详情展示和诊断包往返测试只允许固定处理建议与指标白名单；注入的路径、密钥、账号和未知指标不会出现。

## 正式发布前门禁稳定性

- 第一次正式发布事务在任何构建或远端写入前停止：50 任务压力测试在高负载下未能在固定 2 秒内观察到首个下载器调用；其余 1,326 项通过。隔离诊断记录首任务 16 ms 进入 active、360 ms 调到下载器，完整业务断言通过，确认是非性能语义等待的环境性超时。
- 压力测试条件等待调整为 10 秒后，隔离用例完整通过；全套复跑中的 500 项 SQLite 落盘断言也通过。
- 全套复跑另外暴露两个既有时间耦合门禁：相册和缩略图“最多 4 并发”合同把协程启动写死为 1 秒，统一调整为 10 秒；慢仓储集成测试把 500 ms 窗口跨越次数写死为最多 4 次，改为注入 60 秒确定性窗口并精确断言初始、合并批次和终态共 3 次写入。
- 三类修复均只修改自动化测试，不改变调度、内容浏览、下载持久化或更新生产逻辑。最终完整自动化为 1,327 项通过，用时 449.76 秒；Ruff 输出 All checks passed!。
- 失败发布事务后独立确认：GitHub main、魔搭 source 仍指向 v0.18.4 发布后源码，双方均不存在 v0.19.0 标签，公开 stable/latest 指针未变化。

## 构建与发布边界

- Windows 构建回归验证 PyInstaller 子进程仅收到受控 PATH，调用者 PATH 在成功、非零退出和异常时均恢复。
- 最终发布事务会重新运行完整自动化、Ruff、冻结程序自检、便携包隐私扫描、安装器生命周期测试和双平台候选逐字节比较。
- 账号、会话、消息、用户文件名、真实下载路径、凭据、真实数据库和发布私钥不得进入验证记录或公开制品。

## 正式发布结果

- scripts/release/release.ps1 -Version 0.19.0 以退出码 0 返回 RELEASE_PUBLISHED v0.19.0。事务内第一轮完整自动化为 1,327 项通过，用时 400.43 秒；构建内第二轮为 1,327 项通过，用时 337.57 秒；两轮 Ruff、PACKAGED_SMOKE_OK 与 INSTALLER_SMOKE_OK 全部通过。
- 发布源码提交为 85068eacec57db0313aec6d93d86396da9b6cab3，两端 v0.19.0 注解标签对象为 1d20e91496abbc0590bebd9f179dce1562e02f6c；GitHub main、魔搭 source 和两端标签剥离提交在发布时均指向同一发布源码提交。
- GitHub Release TelegramDownloader 0.19.0 已公开、不是草稿或预发布，共 7 个 uploaded 资产：https://github.com/lx3559359/TelegramDownloader/releases/tag/v0.19.0 。
- 魔搭公开 stable/latest.json 端点返回 HTTP 200 和版本 0.19.0，版本清单返回 HTTP 200、stable 通道及 release-2026-01 密钥编号；补齐项目根存储环境后的 SDK 指针校验退出码为 0。
- 发布事务从 GitHub 草稿和魔搭候选回下载全部资产并逐字节比较通过；正式清单使用内置 release-2026-01 公钥完成 Ed25519 独立验签，清单中的便携包和安装器大小及 SHA-256 由应用更新合同再次验证通过。
- GitHub Actions 对发布源码提交的 verify 工作流成功：https://github.com/lx3559359/TelegramDownloader/actions/runs/33388264536 。

正式发布资产：

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| TelegramDownloader-0.19.0-win-x64-portable.zip | 67,659,772 | 46761921B71A59040D105587281094F36A5ECFFFB38D1F21FE5CBAA17EFC2330 |
| TelegramDownloader-0.19.0-win-x64-setup.exe | 48,282,708 | 83E54838FCE2130F63033834201E71A004F367D85821CCC72628D058B24B2781 |
| TelegramDownloader-0.19.0-source.zip | 2,531,003 | 70262761BA325C8894F1D145051A6B6E8B580BC2DF33651ACC3CECE12CAD338C |
| release-notes.md | 3,232 | 0B4B24E302BCA1C3B93031B5D26DFC98ECEB65BCFBD624351110F8CACEB56FD6 |
| update-manifest.json | 7,350 | C08ADA7B89DEA4E2D4FC5E4D983F5553F49CC80CC072A6F8B59F86FB2D1E9F83 |
| update-manifest.sig | 89 | 96E253490017B33D4AFF8A3669135BF579416C429AB72CC6D65A38504F8A82B7 |
| latest.json | 58 | 66200016DDCF92EE984D0F32C5B969BB1FC90AD8A144F553C05447849C697626 |

本验证记录的公开证据提交位于发布标签之后；只前移 GitHub main 和魔搭 source，不移动 v0.19.0 标签。
