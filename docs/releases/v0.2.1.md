# TelegramDownloader 0.2.1

本版本修复 Telegram 扫码登录窗口中的二维码裁切问题，并继续提供便携包、非 C 盘安装包与签名在线更新。

- 二维码按矩阵模块数选择不超过 300 像素的整数倍尺寸，避免插值模糊。
- 登录窗口使用固定的 300 × 300 二维码视口，完整显示四边白色静区。
- 切换到二维码页面后重新计算窗口尺寸，避免旧页面高度约束导致上下裁切。
- API Hash、代理密码、DPAPI 会话和现有下载数据格式保持不变，无需重新登录或迁移。
- 应用数据、日志、更新缓存和下载内容继续保存在应用/项目目录，不会持久占用 C 盘。
